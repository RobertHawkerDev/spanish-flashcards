import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Profile',
  description: '',
};

export default function ProfilePage() {
  return (
    <div>
      <h1>Profile</h1>
    </div>
  );
}
